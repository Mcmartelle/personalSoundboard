require.config({
    paths: {
        'header': 'app/components/core/header/index',
        'footer': 'app/components/core/footer/index',
    }
});

define(['app', 'header', 'footer'], function(app) {
    app.controller('CreateController', function() {

    });
});